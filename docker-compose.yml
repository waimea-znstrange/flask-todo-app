services:
  web:
    build: .
    ports:
      - "5001:5001"
    volumes:
      - .:/app        # live-reloads code changes without rebuilding
    env_file:
      - .env          # picks up the existing .env file automatically
    tty: true         # Enable colour output
